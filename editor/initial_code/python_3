def detectar_repeticoes(lista):
    # Seu código aqui
    return None

print("Example:")
print(detectar_repeticoes([1, 2, 2, 3]))

# As funções assert são utilizadas para verificar se o resultado da função chamada é o esperado
assert detectar_repeticoes([1, 2, 2, 3]) == True
assert detectar_repeticoes([1, 2, 3]) == False
assert detectar_repeticoes(["a", "b", "b", "c"]) == True

print("Seu código passou pelos testes, clique em Check para avançar!")

